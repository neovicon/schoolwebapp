// The Student dashboard re-uses the existing DashboardHome component
// which contains the full design system showcase (StatCards, DataTable,
// Accordion, Tabs, Modal, Stepper, Form) — exactly what students see.
export { DashboardHome as StudentDashboard } from '../../../pages/DashboardHome';
